# ????????????
data(mtcars)
plot(mtcars)
# ????????????
model <- lm(mpg ~ cyl + wt, data = mtcars)

# ????????????
summary(model)
# ??????
residuals <- model$residuals

# ????????????
qqnorm(residuals)
qqline(residuals)

shapiro.test(residuals)
#??????????????????????????????p?????? 0.06341?????????????????????????????????????????????

# ????????????????????????
library(car)
leveneTest(residuals ~ as.factor(mtcars$gear), data = mtcars)
#???????????????p??????0.4516??????????????????????????????????????????????????????
# ????????????
library(gvlma)
gvmodel <- gvlma(model)
summary(gvmodel)
# ?????????????????????
library(lmtest)
dwtest(model)
